<h1>Welcome {{ user.firstName}}!</h1>

<p>
    You signed up as {{ user.firstName }} the following email:
</p>
<p><code>{{ user.email }}</code></p>

<p>
    <a href="{{ absolute_url(path('app_register_validate', {'token': user.token})) }}">Click here to activate your account</a>
    (this link is valid until {{ expiration_date|date('F jS h:i:s') }})
</p>
