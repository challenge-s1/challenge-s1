doctrine:
    dbal:
        # "TEST_TOKEN" is typically set by ParaTest
        # dbname_suffix: '_test%env(default::TEST_TOKEN)%'
        dbname: api
        host: localhost
        port: 5432
        user: api-platform
        password: "!ChangeMe!"
        driver: "pdo_pgsql"
        charset: utf8
